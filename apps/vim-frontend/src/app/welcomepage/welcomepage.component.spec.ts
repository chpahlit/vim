import { WelcomepageComponent } from './welcomepage.component';

describe('WelcomepageComponent', () => {
  let component: WelcomepageComponent;

  beforeEach(() => {
    component = new WelcomepageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
